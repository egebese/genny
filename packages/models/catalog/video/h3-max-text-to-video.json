{
  "endpointId": "minimax/h3-max/text-to-video",
  "family": {
    "id": "h3-max",
    "name": "MiniMax H3 Max"
  },
  "modality": "video",
  "group": "Text to Video",
  "displayName": "MiniMax H3 Max Text to Video",
  "description": "MiniMax H3 Max writing video from a prompt alone, up to 15 seconds. Renders in about the length of the clip and costs a fraction of a cent, so it is the one to iterate on.",
  "sortOrder": 7,
  "pricing": {
    "unit": "seconds",
    "unitPriceUsd": 0.04,
    "note": "fal publishes this per second of output video: $0.025 at 480P and $0.04 at 768P. Those are promotional launch rates and fal's own page says the discount ends September 1 2026, after which 480P is $0.05 and 768P is $0.08. Revisit then. genmedia reports $0.00017 per compute second for this endpoint, which is not what a request is charged: an earlier version of this file took that row for the price and resold the model at $0.00021 per second, roughly a two hundredth of its cost. The published prose is the authority here, and this note exists so the waiver in catalog-check does not hide the same mistake twice.",
    "scale": {
      "field": "resolution",
      "factors": {
        "480P": 0.625
      }
    }
  },
  "creditMultiplier": 1.25,
  "inputs": [
    {
      "name": "prompt",
      "type": "string",
      "label": "Prompt",
      "required": true
    },
    {
      "name": "duration",
      "type": "integer",
      "label": "Length",
      "default": 5,
      "min": 5,
      "max": 15
    },
    {
      "name": "resolution",
      "type": "enum",
      "label": "Resolution",
      "default": "768P",
      "enum": ["480P", "768P"]
    },
    {
      "name": "aspect_ratio",
      "type": "enum",
      "label": "Aspect",
      "default": "16:9",
      "enum": ["21:9", "16:9", "4:3", "1:1", "3:4", "9:16"]
    },
    {
      "name": "prompt_expansion_mode",
      "type": "enum",
      "label": "Prompt expansion",
      "default": "balanced",
      "required": true,
      "enum": ["disabled", "balanced", "quality"]
    },
    {
      "name": "seed",
      "type": "integer",
      "label": "Seed"
    }
  ],
  "references": [],
  "capabilities": {
    "maxOutputs": 1
  },
  "markUrl": "/models/marks/minimax.svg",
  "artUrl": "/models/art/minimax-h3-max-text-to-video.webp"
}
