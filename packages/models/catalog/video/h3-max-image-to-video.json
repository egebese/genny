{
  "endpointId": "minimax/h3-max/image-to-video",
  "family": {
    "id": "h3-max",
    "name": "MiniMax H3 Max"
  },
  "modality": "video",
  "group": "Image to Video",
  "displayName": "MiniMax H3 Max Image to Video",
  "description": "MiniMax H3 Max animating a still, up to 15 seconds. Give it one image for a start frame, or two to animate from the first to the last.",
  "sortOrder": 8,
  "pricing": {
    "unit": "seconds",
    "unitPriceUsd": 0.00021,
    "note": "fal bills this per compute second ($0.00017), which nobody can know before the job runs, so the catalog resells it per second of output. Measured at 768P: 5s cost $0.00094, 10s $0.00178, 15s $0.00313, which is $0.000209 per output second at its worst. genmedia therefore reports a different unit here on purpose.",
    "scale": {
      "field": "resolution",
      "factors": {
        "480P": 0.6
      }
    }
  },
  "creditMultiplier": 1.25,
  "inputs": [
    {
      "name": "prompt",
      "type": "string",
      "label": "Prompt",
      "required": true
    },
    {
      "name": "duration",
      "type": "integer",
      "label": "Length",
      "default": 5,
      "min": 5,
      "max": 15
    },
    {
      "name": "resolution",
      "type": "enum",
      "label": "Resolution",
      "default": "768P",
      "enum": ["480P", "768P"]
    },
    {
      "name": "prompt_expansion_mode",
      "type": "enum",
      "label": "Prompt expansion",
      "default": "balanced",
      "required": true,
      "enum": ["disabled", "balanced", "quality"]
    },
    {
      "name": "seed",
      "type": "integer",
      "label": "Seed"
    }
  ],
  "references": [
    {
      "field": "image_url",
      "role": "start-frame",
      "accepts": ["image"],
      "required": true,
      "array": false,
      "maxCount": 1,
      "token": "strip"
    },
    {
      "field": "end_image_url",
      "role": "end-frame",
      "accepts": ["image"],
      "required": false,
      "array": false,
      "maxCount": 1,
      "token": "strip"
    }
  ],
  "capabilities": {
    "maxOutputs": 1
  },
  "markUrl": "/models/marks/minimax.svg",
  "artUrl": "/models/art/minimax-h3-max-image-to-video.webp"
}
