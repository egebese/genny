node_modules/
.next/
dist/
coverage/
*.tsbuildinfo
.turbo/
.env
.env.local
.env*.local
!.env.example
.DS_Store
playwright-report/
test-results/
blob-report/
.playwright/
docker/volumes/
apps/web/.env
next-env.d.ts

# Design exploration: generated snapshots, renders and galleries. Multi-megabyte
# and regenerable, so it stays local.
.fal-concepts/

# Agent research dumps. Provenance for a change, not the change: whatever
# survives review lands in the catalog, and the rest goes stale in a week.
.research/
